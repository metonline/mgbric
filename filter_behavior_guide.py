#!/usr/bin/env python3
"""
Quick visual reference for board ranking filter behavior
"""

print("""
╔════════════════════════════════════════════════════════════════════════════╗
║                    BOARD RANKINGS - FILTER BEHAVIOR                         ║
╚════════════════════════════════════════════════════════════════════════════╝

FILTER HIERARCHY:
═══════════════════════════════════════════════════════════════════════════

    📅 DATE FILTER
    └─ Loads available events for that date
       │
       ├─ SCENARIO A: 1 Event on Date
       │  └─ Auto-select event
       │     └─ Hide event picker
       │        └─ Load board rankings immediately
       │
       └─ SCENARIO B: Multiple Events on Date
          └─ Show event picker dropdown
             └─ User selects event
                └─ Load board rankings


FILTER STATE MACHINE:
═══════════════════════════════════════════════════════════════════════════

[Initial Load]
     ↓
[Load board_results.json] ─→ Creates dateToEventsMap
     ↓
[Populate date picker with available dates]
     ↓
[Select first/today's date if available]
     ↓
[Trigger handleDateChange()]
     ↓
     ├─→ [1 event] ─→ Auto-select ─→ Hide event picker ─→ Load rankings
     │
     └─→ [Multiple] ─→ Show event picker ─→ Wait for selection


USER INTERACTIONS:
═══════════════════════════════════════════════════════════════════════════

1. CHANGE DATE
   ┌────────────────────────────────────────┐
   │ User changes date in date picker       │
   └────────────────────────────────────────┘
                    ↓
   ┌────────────────────────────────────────┐
   │ handleDateChange() executes            │
   └────────────────────────────────────────┘
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
   [1 Event]            [Multiple Events]
        ↓                       ↓
   Auto-select         Show dropdown
        ↓                       ↓
   Load rankings       User selects
        ↓                       ↓
   (Done)             Load rankings


2. CHANGE EVENT (Multi-event scenario)
   ┌────────────────────────────────────────┐
   │ User selects event from dropdown       │
   └────────────────────────────────────────┘
                    ↓
   ┌────────────────────────────────────────┐
   │ handleEventChange() executes           │
   │ - Set eventId = selected event         │
   │ - Reset board to 1                     │
   │ - Update tournament info               │
   └────────────────────────────────────────┘
                    ↓
   ┌────────────────────────────────────────┐
   │ Load board rankings                    │
   └────────────────────────────────────────┘


3. CHANGE BOARD
   ┌────────────────────────────────────────┐
   │ User changes board number              │
   │ (or uses Previous/Next buttons)        │
   └────────────────────────────────────────┘
                    ↓
   ┌────────────────────────────────────────┐
   │ handleBoardChange() executes           │
   │ (or changeBoard(delta))                │
   │ - Set boardNum = selected number       │
   └────────────────────────────────────────┘
                    ↓
   ┌────────────────────────────────────────┐
   │ Load board rankings                    │
   └────────────────────────────────────────┘


DATA FLOW EXAMPLE:
═══════════════════════════════════════════════════════════════════════════

┌─ board_results.json ──────────────────────────────┐
│ {                                                  │
│   "events": {                                      │
│     "404155": {"name": "Event 404155", ...},      │
│     "404197": {"name": "Event 404197", ...},      │
│     "405728": {"name": "Event 405728", ...}       │
│   },                                              │
│   "boards": { ... }                              │
│ }                                                  │
└───────────────────────────────────────────────────┘
                    ↓
┌─ loadEventsList() ────────────────────────────────┐
│ Processes events and builds maps                  │
└───────────────────────────────────────────────────┘
                    ↓
┌─ dateToEventsMap ─────────────────────────────────┐
│ {                                                  │
│   "2026-01-25": ["404155", "404197"],            │
│   "2026-01-24": ["405728"],                      │
│   "2026-01-23": ["404821"]                       │
│ }                                                  │
└───────────────────────────────────────────────────┘
                    ↓
┌─ Populate date picker ────────────────────────────┐
│ Available dates: 2026-01-25, 2026-01-24, ...     │
└───────────────────────────────────────────────────┘
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
   Select 2026-01-25      Select 2026-01-24
        ↓                       ↓
   Found 2 events         Found 1 event
        ↓                       ↓
   Show event picker    Auto-select 405728
        ↓                       ↓
   User picks one        Load rankings
        ↓
   Load rankings


API CALLS INVOLVED:
═══════════════════════════════════════════════════════════════════════════

1. Initial page load:
   GET /board_results.json
   ↓ (Parse and build date map)
   ↓ (Auto-select event if applicable)
   GET /api/hand-data?event=404155&board=1
   GET /api/board-results?event=404155&board=1
   ↓ (Render board diagram and rankings)


2. When user changes date:
   (No additional API calls, uses cached data)
   ↓ (May auto-load if 1 event)
   GET /api/hand-data?event=EVENT&board=1
   GET /api/board-results?event=EVENT&board=1


3. When user changes event (from dropdown):
   (No additional API calls, uses cached data)
   GET /api/hand-data?event=SELECTED_EVENT&board=1
   GET /api/board-results?event=SELECTED_EVENT&board=1


4. When user changes board:
   (No additional API calls, uses cached data)
   GET /api/hand-data?event=EVENT&board=NEW_BOARD
   GET /api/board-results?event=EVENT&board=NEW_BOARD


TEMPLATE STATES:
═══════════════════════════════════════════════════════════════════════════

┌─ Initial State ───────────────┐
│ 📅 Date:    [ auto-selected ] │
│ 🏆 Event:   (hidden)          │
│ 🎴 Board:   [1]              │
│ Status:    Loading...        │
└───────────────────────────────┘


┌─ After Date Select (1 event) ─┐
│ 📅 Date:    [2026-01-25]      │
│ 🏆 Event:   (hidden)          │
│ 🎴 Board:   [1]              │
│ Status:    Ready - Auto      │
│            selected event    │
│ Display:   Hand + Rankings   │
└───────────────────────────────┘


┌─ After Date Select (Multi) ───┐
│ 📅 Date:    [2026-01-25]      │
│ 🏆 Event:   [Seç ▼]          │
│ 🎴 Board:   [1]              │
│ Options:   Event 404155       │
│            Event 404197       │
│ Display:   (empty)            │
│ Status:    Waiting for event  │
└───────────────────────────────┘


┌─ After Event Select ──────────┐
│ 📅 Date:    [2026-01-25]      │
│ 🏆 Event:   [404155 ▼]        │
│ 🎴 Board:   [1]              │
│ Status:    Ready             │
│ Display:   Hand + Rankings   │
│ Info:      Event 404155      │
│            25.01.2026        │
└───────────────────────────────┘


CACHE BEHAVIOR:
═══════════════════════════════════════════════════════════════════════════

✓ board_results.json  → Cached in allEventsData
✓ dateToEventsMap    → Built once, reused for all filters
✓ API responses      → DOM updated but not cached
  (Board diagram and rankings fetch fresh each time)


PERFORMANCE METRICS:
═══════════════════════════════════════════════════════════════════════════

Event:                          Time:
──────────────────────────────────────
Initial page load             ~500ms
Load board_results.json       ~200ms
Parse & build date map        ~150ms
Select date (auto-select)     <50ms
Select event (manual)         <50ms
Change board                  <50ms
Load rankings API             ~100-200ms
Render board diagram          ~100-150ms
────────────────────────────────────────
Total first load              ~700ms
Filter change                 ~150-300ms


EDGE CASES HANDLED:
═══════════════════════════════════════════════════════════════════════════

1. No events for selected date
   → Event picker hidden
   → No ranking displayed
   → User prompted to select different date

2. Event data missing
   → Display "Event XXXX" format
   → Still loadable if board data exists

3. Board not found
   → API returns empty results
   → Rankings table shows no data
   → No errors (graceful fallback)

4. Date picker not supported (old browsers)
   → Falls back to text input
   → User enters date manually (dd.mm.yyyy)
   → System validates and loads

5. Multiple rapid filter changes
   → Only latest request processed
   → Previous requests ignored
   → No race conditions


═════════════════════════════════════════════════════════════════════════════

For more details, see: BOARD_RANKING_FILTERS.md
""")
